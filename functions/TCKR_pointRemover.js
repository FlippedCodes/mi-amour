module.exports.run = async (ID, client, type) => {
  // interval every 5 min
  // if time is longer then pointLifetime: remove points from user with FUNC_pointManager
};

module.exports.help = {
  name: 'TCKR_pointRemover',
};
